rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // TEMPORARY DEBUG: allow all reads everywhere
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
